from easyai.utility.registry import Registry

REGISTERED_CLS_MODEL = Registry("cls_model")
REGISTERED_DET2D_MODEL = Registry("det2d_model")
REGISTERED_SEG_MODEL = Registry("seg_model")
REGISTERED_DET3D_MODEL = Registry("det3d_model")
REGISTERED_POSE2D_MODEL = Registry("pose2d_model")

REGISTERED_SR_MODEL = Registry("sr_model")

REGISTERED_GAN_MODEL = Registry("gan_model")

REGISTERED_MULTI_MODEL = Registry("multi_model")
